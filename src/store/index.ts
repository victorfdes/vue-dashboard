import Vue from 'vue';
import Vuex from 'vuex';
import AppModule from './app.module';

Vue.use(Vuex);

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    AppModule,
  },
});
